TASK 13: Update theme.rs with dual palettes
==========================================
Date: Fri Apr 10 2026
Status: COMPLETED

CHANGES MADE:
-------------

1. Added ColorPalette struct with scheme-specific colors:
   - active_border, inactive_border, searching_active_border
   - selected_line_bg, inactive_view_selected_line_bg
   - options, cherry_picked_commit_bg/fg, marked_base_commit_bg/fg
   - options_fg, unstaged_changes

2. Added dark_palette() function:
   - active_border: ["green", "bold"] (high contrast on dark)
   - inactive_border: ["default"]
   - searching_active_border: ["cyan", "bold"]
   - selected_line_bg: ["blue"]
   - inactive_view_selected_line_bg: ["bold"]
   - Cherry picked: ["magenta"] bg
   - Marked base: ["cyan"] bg
   - unstaged_changes: ["red"]

3. Added light_palette() function:
   - active_border: ["blue", "bold"] (visible on light bg)
   - inactive_border: ["black"]
   - searching_active_border: ["magenta", "bold"]
   - selected_line_bg: ["cyan"]
   - inactive_view_selected_line_bg: ["underline"]
   - Cherry picked: ["red"] bg
   - Marked base: ["green"] bg
   - unstaged_changes: ["red"]

4. Updated theme functions to be scheme-aware:
   - active_border_color(config) - uses palette when config empty
   - inactive_border_color(config) - uses palette when config empty
   - searching_active_border_color(config) - uses palette when config empty
   - selected_line_bg_color(config) - uses palette when config empty
   - inactive_view_selected_line_bg_color(config) - uses palette when config empty
   - cherry_picked_commit_text_style() - uses palette when configs empty
   - marked_base_commit_text_style() - uses palette when configs empty
   - options_color(config) - uses palette when config empty
   - options_fg_color(config) - uses palette when config empty
   - unstaged_changes_color(config) - uses palette when config empty

5. Backward compatibility maintained:
   - Function signatures unchanged
   - User-provided config values take precedence
   - Falls back to scheme-aware palette only when config is empty
   - Defaults to Dark scheme when detection fails

6. Existing ColorScheme enum and terminal_color_scheme() detection
   preserved and used by get_theme_palette()

VERIFICATION:
-------------
- No compilation errors in theme.rs or style module
- No clippy warnings in theme.rs
- Pre-existing errors in state.rs (Hash trait) are unrelated to this change

FILE MODIFIED:
--------------
crates/app/src/style/theme.rs
