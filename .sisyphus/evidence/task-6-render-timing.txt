# Task 6: Render Timing Debug Flag - Evidence

## Implementation Summary

### 1. Config Option Added (crates/config/src/lib.rs)

Added `DebugConfig` struct with `render_timing: bool` field:

```rust
pub struct DebugConfig {
    pub render_timing: bool,
}

impl Default for DebugConfig {
    fn default() -> Self {
        Self {
            render_timing: false,
        }
    }
}
```

Added to `AppConfig`:
- Field: `#[serde(alias = "debug")] pub debug: DebugConfig`
- Default initialization: `debug: DebugConfig::default()`

### 2. Render Timing Instrumentation (crates/tui/src/lib.rs)

Added to `render()` method (line 442):
```rust
self.log_render_timing(started_at.elapsed());
```

Added helper methods with `#[cfg(debug_assertions)]` guards:

```rust
#[cfg(debug_assertions)]
fn log_render_timing(&self, elapsed: std::time::Duration) {
    if self.config.debug.render_timing {
        let ms = elapsed.as_millis() as u64;
        if ms > 16 {
            eprintln!("WARN: Render took {}ms (target <16ms)", ms);
        }
    }
}

#[cfg(not(debug_assertions))]
fn log_render_timing(&self, _elapsed: std::time::Duration) {}
```

### 3. Verification

- Cargo check passed: `cargo check -p super-lazygit-tui -p super-lazygit-config`
- Warnings are pre-existing (not from changes)
- Debug-only code is compiled out in release builds via `#[cfg(debug_assertions)]`

### Acceptance Criteria Status

| Criterion | Status |
|-----------|--------|
| Debug build logs render time | ✅ Config option `debug.render_timing: true` enables timing |
| Warning logged when render exceeds 16ms | ✅ `eprintln!("WARN: Render took {}ms (target <16ms)", ms)` |
| Release build has no timing overhead | ✅ `#[cfg(debug_assertions)]` excludes code from release |

### Usage

Add to config.toml:
```toml
[debug]
render_timing = true
```
