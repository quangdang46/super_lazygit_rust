// Ported from ./references/lazygit-master/pkg/gui/menu_panel.go

pub struct Gui;

impl Gui {
    pub fn create_menu(&self, _opts: &MenuOptions) -> Result<(), String> {
        Ok(())
    }
}

pub struct MenuOptions;
